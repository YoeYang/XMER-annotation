# 项目工作指南

这是本项目的总规则文件。每次 Codex 在项目目录中开始工作时，先读取本文件，再读取项目根目录下的 `handover.md`，了解当前状态、已完成内容、待办事项和最近的设计决定。

## 通信规范

1. 全部使用中文回复。
2. 每次回复前使用固定称呼“Yoe”开头。

## 编码规范

3. 不写兼容性代码，除非用户主动要求或明确允许。
4. 写代码前，先用简短文字说明实现方案、涉及文件、主要取舍和验证方式，等待用户批准后再动手。
5. 需求存在影响实现的歧义时，先提问澄清，再写代码。

## 代码质量

6. 写完代码后，列出已经验证的边缘情况和测试用例，并说明测试结果。
7. 如果一次修改超过 3 个文件，先拆分为较小任务，逐步完成和验证。
8. 出现 Bug 时，先写出能够稳定复现问题的测试，再修复 Bug。

## 迭代优化

9. 每次被用户纠正后，先反思具体原因，说明下一步如何避免同类错误，再继续工作。
10. 保持高效。如果陷入长时间思考或循环尝试，立即拆成更小的步骤，依次执行并输出阶段性总结。

## 项目协作规则

11. 修改前先检查当前工作区和 Git 状态，保留用户已有修改。
12. 任何数据格式、时间轴、标注流程或实验协议的改变，都要同步记录到 `handover.md`。
13. 连续标注的原始采样数据必须保留，后期平滑、重采样、延迟校准和多人聚合不能覆盖原始记录。
14. 媒体时间必须以播放器的媒体时间为准，不能用网页经过的墙上时间替代。
15. 涉及标注数据的功能，优先采用可恢复、可导出、可追踪的设计，并为每次标注保留任务编号、标注者编号、模态和尝试编号。

## 项目环境约定

16. 本项目如需配置开发或运行环境，必须在 `D:\anaconda\envs` 下建立独立、干净的项目专用环境，默认路径为 `D:\anaconda\envs\xmer-annotation`。
17. 不复用其他项目环境，不向 Conda base 环境或系统全局环境安装本项目依赖。Node.js、Python 等运行时按实际需要放入项目专用环境；前端 npm 包保持项目本地安装，不做全局安装。
18. 安装、运行和验证命令必须明确使用该项目环境，并在 `handover.md` 中记录实际环境路径、运行时版本和启动方式。若目标路径已存在，先核实用途，不覆盖或清空既有环境。
