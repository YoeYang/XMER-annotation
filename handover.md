# 连续情感标注网页项目交接记录

## 当前状态

第一版标注网页已经实现并通过自动化验证。当前包含 React 前端、四种演示任务、10 Hz 连续采样、IndexedDB 本地保存、Submit／Update 版本链、刷新恢复、JSON 导出和响应式页面；尚未建立云端后端或数据库。

下一阶段是结合实际素材检查界面与任务清单，然后进入第二版云端数据管理与调度。云端阶段需要实现标注者独立网址、服务器自动保存、跨设备进度恢复、样本分配和管理员访问全部结果。本地保存不能作为最终云端功能的替代。

## 持久项目约定：独立开发环境

用户于 2026-09-10 指定：本项目需要的环境统一配置到 `D:\anaconda\envs` 下，使用独立、干净的项目专用环境。此约定已同步写入项目 `AGENTS.md`，后续会话读取项目规则时继续遵守。

- 默认环境路径：`D:\anaconda\envs\xmer-annotation`，创建前核实该路径是否已存在及其用途，不覆盖既有环境。
- Node.js、Python 等运行时按当前开发阶段所需配置，不复用其他项目环境，不向 Conda base 或系统全局环境安装项目依赖；npm 包在项目目录本地安装。
- 环境已创建于 `D:\anaconda\envs\xmer-annotation`，包含 Node.js 22.23.2、npm 10.9.8 和 FFmpeg 9.0.1。npm 包安装在项目 `node_modules`，未做全局安装。
- 开发、构建和测试通过 `D:\anaconda\Scripts\conda.exe run --prefix D:\anaconda\envs\xmer-annotation ...` 明确使用项目环境，详细命令见 `README.md`。

## 三版开发路线图

根据界面优先、随后云端管理、最后曲线校准的顺序，开发计划整理为以下三版。原初始提示词中的“第二版曲线校准”顺延至第三版；第一版现已完成，第二版尚未开始。

| 版本                       | 目标                                                     | 主要交付与验收                                                                                                                                                                            |
| -------------------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V1：标注界面与本地完整流程 | 在本地完成真实可用的标注流程，验证交互与原始数据         | 样本目录与进度、四种材料、二维连续标注、倍速及媒体时间采样、独立轮次、完成时间、自动保存、Submit／Update、本地恢复、JSON 导出；能完整走通选择样本到提交、重标、更新及重新打开恢复的流程。 |
| V2：云端数据管理与调度     | 部署到统一云端服务器，支持多位标注者持续工作及管理员管理 | 云端自动保存、每位标注者独立网址、样本分配、跨浏览器进度恢复、管理员查看和导出全部结果、提交历史、访问隔离、失败重试和重复请求去重；通过 Docker 与 Nginx 部署，保存成功以服务器确认为准。 |
| V3：曲线回放与延迟校准     | 支持检查轨迹与媒体的时间关系，为研究分析提供可追踪数据   | Valence／Arousal 曲线、媒体与本人轨迹同步回放、前后平移校准、保存偏移方向和偏移量、独立保存原始及校准结果、明确越界缺失值策略；正式标注时不展示其他模态曲线。                             |

所有版本均保留原始采样数据与历史轮次，Update 只形成新的提交版本，不覆盖旧轨迹。V1 的保存与恢复仅限当前浏览器，界面明确标识本地保存；V2 才提供云端保存与专属网址恢复。

### 第一版已实现方案

- 技术栈采用 React、TypeScript、Vite、普通 CSS 和 IndexedDB，存储已拆成 `AnnotationRepository` 接口，供 V2 接入后端。
- 页面由样本目录、媒体播放区、二维标注区和轮次／提交状态区组成。首次点击确定坐标并从零开始正式标注，默认 10 Hz，采样器独立于鼠标事件并读取播放器 `currentTime`。
- 文本按与原音频说话位置对齐的时间戳呈现，支持逐段或细粒度片段；当前在句内按字／词时间戳逐步显示，以跟随语速与停顿。第一版接收预先准备的时间戳，不实现自动语音对齐。仅文本任务使用内存生成的 PCM 无声媒体，由原生音频播放器 `currentTime` 驱动，不播放原音频。
- 本地按约 1 秒间隔增量保存原始记录，样本完成后立即保存草稿并记录 `completed_at`；首次 Submit 记录 `submitted_at`，后续 Update 记录 `updated_at` 并保留版本关系。刷新后恢复已持久化结果和进度，中断轮次保留并明确标记。保存失败时可重试；暂停后还可导出内存中尚未落盘的尾部，并通过 `unsaved_attempt_ids` 明确标记。
- 文件按工程配置（`package.json`、锁文件、`index.html`、`tsconfig.json`、`vite.config.ts`）、页面入口与样式（`src/main.tsx`、`src/App.tsx`、`src/styles.css`）、组件（`src/components/`）、媒体／采样／轮次逻辑（`src/core/`）、存储与导出（`src/storage/`）、类型与配置、任务及演示素材（`public/`）、测试（`tests/`）和运行说明（`README.md`）拆分。
- 已完成工程骨架、界面布局、媒体与文本时间轴、连续采样与轮次、本地保存与提交更新，以及整体验收。
- 自动化验证覆盖四种材料、文本语速与停顿、坐标边界、静止和离开区域继续采样、倍速、暂停／缓冲／后台停采、结束停采、重复开始、预览后正式标注、刷新恢复、历史保留、重复提交、导出一致性、加载失败和保存失败。核心逻辑 17 个用例通过；浏览器端 9 个用例通过；TypeScript 检查及生产构建通过。
- 当前目录仍未初始化 Git。仓库没有真实实验素材，`public/media` 与 `public/transcripts` 中的文件均为明确标识的演示材料，只用于验证操作；接入真实素材后需要复核格式、时长与跨模态时间轴。

## 最新更新日志

### 2026-09-10，完成第一版标注网页

完成样本目录、四种材料播放器、二维 Valence／Arousal 标注、媒体时间采样、文本时间戳呈现、本地自动保存、草稿与中断恢复、Submit／Update、历史轮次和 JSON 导出。页面支持桌面与窄屏，提供明确的本地保存状态、加载错误和操作指南。

原始采样采用默认 10 Hz 定时触发，采样点的 `media_time` 直接读取原生播放器 `currentTime`；鼠标事件只更新当前坐标。暂停、缓冲、页面隐藏和播放结束会停止采样，结束时补记最后一个真实媒体时间点。预览轮次不产生正式样本，预览结束后可直接开始正式标注。

验证结果：Vitest 3 个测试文件、17 个用例全部通过；Playwright 9 个浏览器用例全部通过；`tsc --noEmit` 与 Vite 生产构建通过。演示素材时长为 12 秒，音频是合成音，文本时间戳用于验证逐步显示，不代表真实语音对齐。

### 2026-09-10，整理三版路线图与第一版编码准备

将开发顺序明确为 V1 标注界面与本地流程、V2 云端数据管理与调度、V3 曲线回放与延迟校准。补充第一版模块拆分、数据和文本时间轴方案、验证范围及运行环境检查结果；尚未开始编码，等待第一版方案批准。

### 2026-09-10，补充样本目录、提交更新与云端管理需求

以下为用户新增要求，本次仅记录需求，不开始前端或后端实现。

1. **样本目录与进度**：页面需要提供待标注样本目录，显示每个样本的标注状态，允许打开样本进行标注或修改。这里的“样本”指一份待标注材料或任务，不是连续轨迹中的单个采样点；数据结构继续区分任务、材料、标注轮次和采样点。
2. **完成时间**：记录每位标注者对每个样本的完成时间，并保留每次完成轮次的时间。设计中区分标注完成时间 `completed_at`、首次提交时间 `submitted_at` 和后续更新时间 `updated_at`；这些时间不能替代采样点的播放器媒体时间 `media_time`。
3. **自动保存**：每个样本采集完成后立即触发保存，即使尚未点击 Submit，也应保留本轮结果作为草稿，以便下次打开读取。自动保存与正式提交是不同状态，界面需要显示保存中、已保存和保存失败，云端保存成功须以服务器确认结果为准。
4. **Submit 与 Update**：首次标注完成后使用 Submit 正式提交；已提交样本再次修改后使用 Update 更新当前提交版本。Update 必须保留此前的原始记录和版本关系，不能覆盖历史轨迹；重新标注仍创建新的 `attempt_id`。重复点击或请求重试不能产生重复提交版本。
5. **统一云端部署**：采集网页部署在用户自有的统一云端服务器，标注结果暂存云端。每次重新打开网址时，从服务器读取该标注者此前的样本目录、标注结果和进度，而不是只依赖原浏览器缓存。
6. **标注者独立网址**：每位标注者获得不同的网址，可直接进入自己的标注任务。后台将访问凭据与标注者及分配样本关联，限制标注者只访问自己的任务和结果；具体网址凭据与访问机制在后台设计阶段确定。
7. **管理员访问与调度**：管理员能够访问所有标注者的标注结果，并查看对应样本、进度、完成时间和提交／更新历史。样本分配、独立网址生成和后台管理的具体界面与调度规则，在后续后台阶段设计。
8. **实施顺序**：先设计标注界面，纳入样本目录、完成时间、保存状态以及 Submit／Update 交互，并预留数据接口；界面设计完成后，再实施云端存储、标注者访问、进度恢复和管理员管理功能。后台尚未接入时，界面演示不得将本地或模拟保存标记为云端保存成功。

后台阶段需验证：完成后未 Submit 的草稿可重新读取；首次 Submit 与后续 Update 状态正确且历史原始数据保留；重新打开专属网址能恢复进度；不同标注者的数据相互隔离，管理员可访问全部结果；断网或保存失败有明确提示并可重试；重复提交不会新增重复版本。

### 2026-09-10，建立第一版需求

目标是开发一个部署在自有云服务器上的连续情感标注网页。标注者分别观看不同模态的材料，在 Valence 和 Arousal 二维方形平面中随播放移动光标。系统按照媒体时间持续采集二维坐标，后期由研究者生成各模态的 Valence 曲线和 Arousal 曲线，用于分析模态间情感不一致和冲突。

最初规划第一版实现基本标注流程、第二版实现曲线校准。现已按上方三版路线图调整：第一版完成界面与本地流程，第二版完成云端数据管理与调度，第三版完成曲线回放、延迟校准和标注轨迹平移。

## 可直接交给 Codex 的初始开发提示词

```text
Yoe，请先读取项目根目录的 AGENTS.md 和 handover.md。当前项目刚开始，请先检查目录，不要立即写代码。先给出一个简短的实现方案，说明技术栈、目录结构、第一版涉及的文件、数据格式、时间同步方法、如何运行和如何验证。等我批准方案后，再开始编码。

项目目标

开发一个可以部署到我自己的云服务器上的连续情感标注网页。第一版先实现基本标注功能，不做复杂的用户管理、研究者后台和曲线校准界面。代码结构需要便于第二版继续扩展。

研究任务

标注者需要根据当前看到或听到的单一模态材料，在 Valence 和 Arousal 二维空间中持续表达对目标人物当前情感状态的感知。系统采集的是原始二维坐标随媒体播放时间变化的序列。后期研究者会根据原始采样数据生成 Valence 和 Arousal 两条时间曲线，并比较不同模态的局部变化、错位和冲突。

第一版的呈现条件

每次只显示一个待标注文件。任务列表可以让标注者选择当前要标注的材料，但正式标注时只能加载一个文件。最终每个被分配的文件都需要完成标注。

需要支持以下四种材料类型：

1. 仅视觉：播放去掉音轨的无声视频。
2. 仅音频：播放音频文件，页面可以显示一个简单的音频播放区域或波形占位区域。
3. 仅文本：文本按照预先提供的时间戳逐段或逐字显示，不能一次性显示完整文本。第一版使用一个清晰、容易维护的 JSON 数据格式，例如包含 start、end 和 text 的时间片段数组。
4. 完整视频：播放带声音的视频。

视频、音频、文本和完整视频必须共享同一媒体时间轴。播放速度可以选择 0.5、0.75、1、1.25 和 1.5 倍，并允许播放、暂停、重新开始和多次播放。改变播放速度只改变实际播放所需的墙上时间，采样时间仍然必须读取播放器的媒体时间 currentTime。

第一版界面

设计一个简洁的桌面优先标注页面。媒体播放区和二维标注区左右并排，默认媒体在左侧，二维标注区在右侧。窄屏时上下排列。

媒体区需要包括：

1. 当前材料名称、材料类型和任务进度。
2. 视频或音频播放器。
3. 播放、暂停、重新开始和播放速度控制。
4. 清晰的媒体进度条和当前时间、总时长。
5. 允许在正式开始前多次播放和熟悉材料。

二维标注区需要包括：

1. 一个正方形二维平面，不使用圆盘。
2. 横坐标为 Valence，从负面到正面。
3. 纵坐标为 Arousal，从低唤醒到高唤醒。
4. 四个角落可以放简短的语义提示，帮助标注者理解方向，但不能用离散情绪标签代替连续坐标。
5. 标注者第一次点击方形中的一个位置后，本轮媒体从 0 秒开始播放，并立即开始记录。
6. 播放期间，标注者移动鼠标来更新当前二维位置。采用最容易稳定实现的方案：不要求一直按住鼠标，鼠标移动到方形内部时更新当前位置；鼠标离开方形时保持最后一个有效位置。也可以支持按住拖动，但不能让是否按住鼠标决定数据是否继续采样。
7. 光标不移动时也必须继续采样，因为这表示情感状态保持稳定。
8. 播放结束时自动停止采样，并显示本轮已完成。
9. 提供“重新标注”功能。重新标注必须创建新的 attempt_id，不能覆盖之前的原始记录。
10. 在正式提交前允许标注者检查当前轮次是否完成，并明确点击提交。

连续数据采集要求

每个采样点至少保存以下字段：

task_id、annotator_id、modality、media_id、attempt_id、sample_index、media_time、wall_time、valence、arousal、is_valid。

要求如下：

1. media_time 必须来自当前播放器的媒体时间。
2. 固定采样间隔，例如 100 毫秒或 10 Hz。请在方案中说明选择的采样频率，并将它设计成容易修改的配置项。
3. 采样过程独立于鼠标移动事件，鼠标事件只更新当前坐标，定时采样器负责记录坐标。
4. 视频、音频、文本和完整视频都使用同一套采样数据结构。
5. 记录原始数据，不在前端直接覆盖平滑、插值或延迟校准结果。
6. 播放暂停、缓冲、切换页面和播放结束时要正确处理采样状态。
7. 同一文件多次播放时，每次都要保留独立的 attempt_id。
8. 记录应可以导出为 JSON 或 CSV，方便后续使用 Python 或 R 处理。

第一版的数据保存

请优先设计一个简单、可扩展的数据保存接口。若完整服务器端保存会使第一版过于复杂，可以先实现本地可运行版本和 JSON 下载，但代码中需要保留后续接入 FastAPI 与 SQLite 或 PostgreSQL 的清晰接口。若实现最小后端，请保证提交后服务器能够保存原始标注记录，并且重复提交不会产生无法区分的重复数据。

最新补充：本地运行和下载仅作为界面开发阶段的过渡。界面设计完成后，需要实施统一云端保存、样本完成后自动保存草稿、首次 Submit、后续 Update、标注者独立网址、重新打开恢复进度，以及管理员访问全部结果。详细要求见最新更新日志，历史原始记录不得被 Update 覆盖。

第三版预留（按最新三版路线图调整）

第三版需要在标注完成后显示由原始采样数据生成的 Valence 和 Arousal 时间曲线。标注者可以同时播放媒体和自己的标注轨迹，根据主观感觉对齐两者，在小范围内将标注轨迹向前或向后平移。平移超出原始时间范围的部分使用明确的缺失值或边界策略处理，不能静默伪造数据。第三版还需要保存原始轨迹、校准后的轨迹、延迟方向和延迟量。

第一版暂时不要实现第三版的延迟校准，但目录结构和数据结构要预留 calibration 字段或独立表。

实现要求

1. 先给实现方案，等待批准后编码。
2. 代码使用清晰的组件和模块，不把所有逻辑写在一个文件中。
3. 不引入没有必要的商业服务或需要会员才能使用的标注平台。
4. 页面必须能够在本地运行，之后可以通过 Docker 和 Nginx 部署到自有云服务器。
5. 媒体文件不要硬编码到组件中，使用任务配置或媒体清单管理。
6. 正式标注页面中不要显示其他模态的标注曲线，避免影响独立判断。
7. 需要处理无效文件、媒体加载失败、没有时长、用户刷新页面、重复点击开始、暂停、缓冲、鼠标离开二维区域和网络中断等情况。
8. 所有动态数据都要有清晰的状态提示，例如等待开始、正在采样、已暂停、已完成、保存成功和保存失败。
9. 代码完成后，先运行项目，再列出边缘情况和测试用例，并报告测试结果。

请先只输出方案，不要创建或修改代码文件。
```

## 第一版的验收标准

1. 可以加载一个无声视频、一个音频文件、一个完整视频和一个带时间戳的文本任务。
2. 标注者点击二维方形后，材料从媒体时间 0 开始播放并进入采样状态。
3. 光标移动会更新 Valence 和 Arousal，光标不动时仍持续产生采样点。
4. 采样点的时间跟随媒体 `currentTime`，改变播放速度不会破坏媒体时间轴。
5. 播放结束后自动结束本轮记录。
6. 重新播放或重新标注不会覆盖之前的 attempt。
7. 可以导出包含原始时间和二维坐标的 JSON 或 CSV。
8. 页面刷新、媒体加载失败和暂停状态有明确反馈。
9. 后续可以在不改变原始数据的前提下增加曲线显示和延迟校准。

## 尚待决定的问题

1. 三个独立模态具体是视觉、音频、文本，还是视觉、音频和完整音视频。
2. 文本时间戳由人工制作、字幕文件提供，还是根据语音自动对齐。
3. 正式实验采用 10 Hz、20 Hz 还是其他采样频率。
4. 标注者点击方形后是否立即开始正式记录，还是先允许一次练习点击。
5. 多次尝试最终采用最后一次、研究者选择的一次，还是保留所有尝试进行质量分析。

---

## 与 `04-conflict-sampling` 的关系（2026-09-10 补，接手方必读）

**本目录（`XMER/05-annotation/`）通过 `git clone` 自 GitHub `YoeYang/XMER-annotation` 同步而来，是新连续 Valence–Arousal 标注项目在 Roihu 上的代码副本；日常开发仍以本地 `D:\anaconda\envs\xmer-annotation` 为主。2026-09-10 起用户明确允许在 Roihu 上直接改动并 commit（部署/取真实样本这类需要 HPC 侧资源的工作），push 到 GitHub 的时机由用户后续指示，不必每次自动同步。**

- **本项目取代什么**：`../04-conflict-sampling/coding_scheme_annotation_platform_archive/` 里的 Label Studio 离散 behavior-cue 标注平台已就绪但**没有正式开标**，标注方式改为本项目的连续二维坐标采集。LS 平台**不废弃**，只是暂停，按其 `handover.md` §1 复活清单可随时接回去。
- **样本从哪来**：待标素材直接复用 `../04-conflict-sampling/0-conflict_sample_selection/data/` 里已筛好的：
  - `annotation_pool_3500.jsonl`（+ `.meta.jsonl/.meta.csv`）—— 主标注池，3500 条
  - `anchor_set_500.jsonl`（+ `.meta.jsonl/.meta.csv`）—— 锚点集，500 条
  - 这两份 `.meta.*` 里已 join 好视频路径、转录、GT sentiment、双 judge（Gemini/GPT-5.6-luna）判定结果，接入 V2 时直接读这些字段即可，不需要重新跑筛选流水线。
  - 素材视频编码问题（308 条 chsims/mustard 编码或无音轨）已在 LS 平台阶段修复过，见该 handover §"视频兼容性修复"；若本项目独立取用原始视频，需要同样注意 mustard 无音轨（音频在同目录 `audio.wav`）。
- **CPM 四维打分**（appraisal theory R/I/C/N）：两边一致同意暂不做，留到主标注完成后由内部受训人员在子集上补做，与本项目无直接关系。
- **不复用的部分**：LS 的账号体系、组织隔离方案，是 LS 专用；阿里云 ECS **实例本身现已复用**（见下方 2026-09-10 部署记录），但服务/容器/数据完全独立。

---

## 2026-09-10（续）— 真实样本 example 集 + 部署上线

**背景**：用户要求跳过手机适配（暂不需要），并把网站推送到云端；示例素材改用 04 筛好的真实冲突样本，而非合成 demo。

**新增真实样本集**（`EXAMPLE-001..004`，`demo:false`）：取 `annotation_pool_3500` 里 `meld_dia11_utt9`（MELD，Phoebe，双 judge 高置信度冲突：text/audio=happy vs video=angry），做成 visual/audio/text/audiovisual 四个单任务，供逐模态对比标注。素材处理（Roihu，`module load ffmpeg/7.1`）：
- 原视频已是 h264/aac，**未重编码**，只 stream copy；音轨从 6 声道(5.1)降混为双声道。
- `visual.mp4`＝去音轨；`audio.wav`＝双声道 PCM；`audiovisual.mp4`＝原始有声版。
- **转录无逐词时间戳**：`public/transcripts/example.json` 的词级时间按字符数在句子时长内近似匀速分配，写了 `alignment_note` 字段说明不是真实语音对齐，仅用于逐词出现的展示节奏。

**部署上线**（**复用 04 的阿里云香港 ECS，同一台机器**，不新开服务器）：
- 访问地址：**https://47.238.255.165.nip.io/annotation/**
- 做法与 `../04-conflict-sampling/coding_scheme_annotation_platform_archive/` 的 `/training/` 静态页同一模式：Caddy `handle_path /annotation* { root * /annotation-static; file_server }`，静态文件由 Docker compose 只读挂载进 caddy 容器（`./annotation-static:/annotation-static:ro`）。**不影响 LS / Postgres / training 页**（部署后已逐一 curl 验证 200）。
- **构建方式**：ECS 上没长期装 Node，用一次性容器 `docker run --rm node:20-alpine sh -c "npm ci && npm run build"`（源码先从 Roihu `rsync` 到 ECS `/opt/xmer-annotation-src/`，构建产物 `dist/` 再 rsync 进 `/opt/xmer-label/annotation-static/`）。
- **踩坑1（构建产物路径）**：Vite 默认打包成绝对路径 `/assets/...`，被 `/annotation*` 前缀剥离后请求打到根路径的 LS 服务，404。**修法**：`vite.config.ts` 加 `base: process.env.VITE_BASE_PATH ?? "/"`（本地开发默认 `/` 不受影响），ECS 构建时传 `VITE_BASE_PATH=/annotation/`。
- **踩坑2（运行时 fetch 路径，用户截图报的 bug）**：`base` 只管 HTML 里的静态资源引用，**代码里手写的 `fetch("/tasks.json")` 和 `task.src`（如 `/media/...`）不会自动跟着 base 走**，仍是根路径绝对路径，同样落到 LS 反代，页面报"样本目录加载失败"。**修法**：加 `resolveAssetPath()`（`src/config.ts`，用 `import.meta.env.BASE_URL` 拼前缀），tasks.json 的 fetch、媒体 `src`、文本 fetch 全部走它。顺带修了 `MediaPanel` 缺 `key={task.task_id}` 导致切任务时播放器 source 不更新的问题（source 只在首次挂载算一次）。**教训：子路径部署时，`base` 和代码里所有运行时请求路径要一起改，前者不覆盖后者。**
- 改动的 ECS 文件（均已备份 `.bak-preannotation`）：`/opt/xmer-label/Caddyfile`、`/opt/xmer-label/docker-compose.yml`（给 caddy 服务加一行只读挂载）。源码副本留在 `/opt/xmer-annotation-src/`，之后有新 commit 需要重新 rsync + 容器构建 + rsync 到 static 目录 + 不需要重启 caddy（静态文件是 bind mount，直接生效；只有 Caddyfile/compose 改动才需要 `docker compose up -d caddy`）。

**Git 身份说明**：Roihu 本地此仓库此前从未配置 git identity，已**仅在本仓库范围**（非 `--global`）设置 `user.email=yyyueyi@outlook.com`。**用户拍板**：Roihu 上可以直接改并 commit，**不必每次都 push 到 GitHub 或同步回本地**，push 时机由用户后续明确指示。当前 Roihu 本地领先 GitHub 一个 commit（`4c12913`），尚未 push。

**当前 tasks.json 状态**：4 个 demo 任务（合成素材）+ 4 个 EXAMPLE 真实样本任务共存，均可在线上直接看到。之后接入 3500 池全量时按同一 schema 扩展 `tasks.json` 即可。

---

## 2026-09-10（续 2）— UI 四项改动（完整视频优先 / 配色 / 侧栏分组 / 曲线图）

用户四条改动（手机版明确不做）：

1. **完整视频排最前**：每个样本内模态顺序 `完整视频 → 仅视觉 → 仅音频 → 仅文本`（先看完整视频在多人场景里确定说话主体）。`tasks.json` 重排，侧栏也强制此序，**默认选中项现在是完整视频**。模态排序常量 `MODALITY_ORDER` 在 `TaskSidebar.tsx`。
2. **标注方形配色+标签**（`AnnotationPad.tsx` + `styles.css`）：左半冷色蓝（负 valence）、右半暖色橙（正），每半上深（高唤醒）下浅（平静）。valence 标签移到左右两侧（左"负面"/右"正面"），arousal 标签上"激动"/下"平静"。删掉四角象限标签和旧的底部/低唤醒轴行。中心十字线颜色改白色半透明以适配新底色。
3. **侧栏分组下拉**（`TaskSidebar.tsx` 重写 + `styles.css`）：按 `source_id` 分组，一样本一行可折叠；展开显示 4 个模态子项，**统一用模态名**（完整视频/仅视觉/仅音频/仅文本），不再显示 demo 的诗意标题。已提交子项打勾，样本头显示 `n/4` 进度、全做完打勾。筛选（全部/待完成/已提交）作用在模态子项层级。默认展开当前选中任务所在的样本组。
4. **情感曲线图**（`scripts/plot_va_curves.py`，独立分析脚本，不属 app）：读 V-A 完整导出，x=时间、y=valence（上正下负），**arousal 编码为 valence 中心线上下填充带的宽度**（越宽越激动，用户拍板此方案，非线条粗细）。产三张：三单模态 / 完整视频 / 四合一。Roihu 无中文字体，轴标用英文。已用 A001 标的 meld_dia11_utt9 四模态生成过样图。

**验证**：`tsc --noEmit` 过、vitest 17/17 过、线上构建 index/JS/CSS/tasks.json/media 全 200。
**⚠️ 未验证 / 待办**：Playwright e2e（`tests/annotation.spec.ts`）**必然失效且未修**——① Roihu 无浏览器跑不了；② 用例强依赖旧扁平侧栏（按标题 `/光影之间/` 等选任务）和"默认任务=视觉 demo"，都被本次改动打破（现在按模态名选、要先展开分组、默认=完整视频；`无效媒体`那条 abort 的是 `visual.mp4` 但默认任务已换成 audiovisual）。**UI 设计定稿后需在本地 dev 环境重写并跑这些 e2e。**

---

## 2026-09-10 收尾 — 当前状态 + 下一步计划

### 存储架构现状（重要，别误会）
标注结果目前**只存在标注者本地浏览器的 IndexedDB**（`storage:"local"`），**云端没有任何标注数据**。ECS 上只托管前端静态文件 + 示例视频，不接收结果。换设备/换浏览器/清站点数据都会丢，唯一备份手段是页面"导出 JSON"。这是 V1 的过渡设计，云端保存是 V2 的核心目标（未开始）。

### 本次 session 落盘的 commit（Roihu 本地，均已 commit）
```
87d5e7f docs: 记录四项 UI/plot 改动 + e2e 待更新
f1f2205 feat: 完整视频优先 / 冷暖 V-A 方形 / 侧栏分组 / V-A 曲线脚本
057e871 docs: 子路径部署路径解析踩坑
c5f9986 fix: 运行时请求路径按 BASE_URL 解析（修"样本目录加载失败"）
4c12913 feat: 真实冲突样本 example 集 + 部署构建配置
b226ee6 feat: initialize XMER annotation V1  ← GitHub 上只有这一个
```
GitHub `YoeYang/XMER-annotation` 目前**只有 b226ee6**；上面 5 个 commit 待 push（Roihu 无 GitHub 凭证，需用户本地 push，命令见下）。

### 下一步计划（按优先级）

**P0 — V2 云端后端（把标注真正存到服务器）**
- 技术选型建议：FastAPI + Postgres，**复用同一台 ECS**（Postgres 容器已在跑，可加个库/schema；Caddy 加 `/annotation/api/*` 反代到后端容器）。
- `AnnotationRepository` 接口已抽象（`src/storage/repository.ts`），V2 加一个 `HttpRepository` 实现即可，前端改动小。
- 后端需覆盖 handover 里列的验收点：草稿自动保存、Submit/Update 版本链不覆盖历史、标注者独立网址、跨设备恢复、管理员汇总、断网重试、重复提交去重。

**P1 — 接入真实标注池**
- 现在只有 4 demo + 4 example（1 个样本）。要把 `04-conflict-sampling/0-conflict_sample_selection/data/annotation_pool_3500`（+500 锚点）批量转成 tasks.json 条目：每样本×4 模态，视频按 example 同法预处理（stereo aac、去音轨出 visual、mustard 合 audio.wav），转录时间戳方案待定（见下）。
- 素材上线前必做全量 ffprobe 扫编码 + 查音轨（04 平台踩过 308 条坑）。

**P2 — 待用户定的口径问题**（handover"尚待决定"里的）
- 采样频率 10/20 Hz；点击后是否先练习点击；多 attempt 取哪次；文本逐词时间戳来源（人工/字幕/强制对齐——example 用的是近似匀速，正式标注需定方案）。

**P3 — 工程收尾**
- Playwright e2e 重写并在本地 dev 环境跑绿（本次未做，见上一段）。
- 曲线图中文字体（要中文版需提供 ttf/otf）。
- 曲线脚本可考虑并入 app 或做成分析工具目录。

### push 到 GitHub 的命令（用户本地或在能访问 GitHub 的机器上跑）
```bash
# 方式 A：在 Roihu 上用 ! 前缀跑（需带 token 的 remote 或 gh auth）
git -C /scratch/project_2017416/yyy2026/XMER/05-annotation push origin main
# 方式 B：本地已 clone 的仓库里 pull 不适用（Roihu 领先），直接从 Roihu 这份 push 最省事
```

---

## 2026-09-11 — V2 设计定稿 + T1 开工

在线版设计规格（会随进度更新，同一链接）：
<https://claude.ai/code/artifact/825d8650-c7c9-4970-9396-f18a1c0aef5e>

### 术语：两个正交的轴（接手方必读，极易混淆）

| 轴 | 1 | 2 | 3 |
| --- | --- | --- | --- |
| **软件版本 V** | 界面与本地流程（已完成） | 云端数据管理（进行中） | 曲线回放与延迟校准 |
| **实验阶段 P** | pilot：8 人 / 300 样本 | training：全体 / 10 样本 | main：≥20 人 / 3500 样本 |

两者都编号 1–3 但**互不对应**。下文统一用 V / P 前缀。

### 三阶段实验设计（2026-09-11 用户确定）

| 阶段 | 标注者 | 样本 | 目的 | 重叠设计 | 软件依赖 |
| --- | --- | --- | --- | --- | --- |
| P1 pilot | 8 | 300 | 验证流程；产出锚点参考标注 | 部分样本双标，估计标注者间一致性 | V2 |
| P2 training | 待定 | 10 | 培训；校准标注细致度 | 全体标同一批 | **V3** |
| P3 main | ≥20 | 3500 | 正式标注 | 300 个 pilot 样本作为锚点按比例散布 | V2 |

- **锚点池由 500 收窄为 300**（2026-09-11 用户决定，为减轻负担）。这 300 同时就是 P1 的标注对象，其 pilot 结果构成 P3 锚点的参考基准。04 原本的 3500+500 之说自此以 300 为准。
- 锚点按 `order_index` **散布插入队列内部**，不得堆在首尾（否则与正常数据处于不同疲劳水平，不可比）。每人队列中的锚点占比暂定 10%，待按单人工作量上限回算。
- **P2 依赖 V3**：培训要求标注者看到自己刚画的曲线，而曲线回放属 V3。V2 交付后可直接开跑 P1 与 P3，P2 必须等 V3。

### 模态呈现顺序返工（重要，推翻 2026-09-10 的决定）

V1 把完整视频排最前（为在多人场景确认说话主体）。但同一标注者随后要标三个单模态——看过完整视频后，单模态标注测的是记忆而非单模态感知，而**模态间不一致正是本课题的因变量**，会被系统性低估。

新顺序：**静帧参考 →（仅视觉 / 仅音频 / 仅文本）→ 完整视频**

说话人识别改由**静帧身份参考**承担：预处理阶段取出目标说话人静态帧，标注时常驻页面提示「按这个人标注」。**静帧属于任务说明，不是被评定的材料**。

连带改动：`tasks.json` 增 `speaker_ref` 字段；`TaskSidebar.tsx` 的 `MODALITY_ORDER` 中 `audiovisual` 由 0 改 3；`MediaPanel.tsx` 常驻展示静帧。三个单模态是否按标注者轮换以抵消顺序效应，待定。

### 静帧参考的生成流水线（数据集预处理，在任何标注开始之前全部跑完）

标注者只消费成品，不参与抽帧、裁切或挑选。

1. `ffmpeg` 抽若干候选帧，供下一步挑出正脸清晰、未被遮挡的一张
2. **Gemini-flash** 批量定位并裁切目标说话人面部（接入方式与额度由用户提供）
3. 与 **MELD 元数据的 speaker 字段**交叉校验，自动过滤明显错配
4. **研究者侧**联系表式网格页核对：一屏铺上百张裁切图并标人名，只点错的，把逐条审查压成扫视
5. 产出 `speaker_ref` 写入 `tasks.json`，随平台上线

规模 3500 + 300。这一步不能省：**静帧指错人，该样本四个模态的标注一起作废**。

### V2 架构要点

- **前端用装饰器而非替换**：新增 `SyncingRepository` 实现同一个 `AnnotationRepository` 接口，内部先写 IndexedDB（预写缓冲，断网可继续标注）再入队推 `HttpRepository`，服务器确认后才算已同步。V1 的 `IndexedDbRepository` 及其测试全部复用。`SessionView.save` 语义从「已落盘」扩为「已同步」，需新增 `pending` 态并显示待上传条数。
- **写操作一律客户端生成 ID + PUT**，天然幂等，这是「重复请求去重」验收点的实现方式。
- **采样按 chunk 追加存储**：每次 checkpoint 写一个不可变块，主键 `(attempt_id, chunk_index)` 即幂等键；导出时按序拼接。避免了按行存的数百万行、也避免了整数组重写。
- **导出格式与 V1 的 `ExportData` 完全一致**，`scripts/plot_va_curves.py` 零改动可用于云端数据；`storage` 字段由 `"local"` 扩为 `"local" | "cloud"`。
- **身份**：`/annotation/?t=<不可枚举 token>`，前端首读后存 sessionStorage 并从地址栏抹掉（防 referer 与截图泄漏），服务端只存 token 哈希。管理端独立 token + Caddy IP 白名单。
- **部署**：复用现有 ECS 与 Postgres 容器，**新建独立库 `xmer_annotation`**，不触碰 Label Studio 的库；Caddy 加 `/annotation/api/*` → `backend:8000`；定时 `pg_dump` 备份。

### 实施拆分 T1–T7

| | 任务 | 验证 | 依赖 |
| --- | --- | --- | --- |
| T1 | 后端骨架与数据模型 | pytest | — |
| T2 | API 实现（幂等、版本链、队列下发） | pytest | T1 |
| T3 | `HttpRepository` + `SyncingRepository` | vitest | 可与 T1/T2 并行（mock） |
| T4 | token 身份接入 + 模态顺序返工 + 静帧展示 | — | T2 T3 |
| T5 | 部署（Dockerfile / compose / Caddy / 备份） | curl | T2 |
| T6 | 管理端只读（进度总览、全量导出） | — | T2 |
| T7 | 管理端写操作（分配 CRUD、链接生成、作废、质量标记） | — | T6 |

### T1 已完成（本次）

新增 `server/`，13 个 pytest 用例全部通过。目录见 `server/README.md`。

守住的核心不变量（改动时勿破坏）：采样块幂等键、乱序块按序还原、提交版本链不覆盖历史、`(annotator_id, task_id, revision)` 唯一、`(annotator_id, task_id, phase)` 唯一（同阶段不重复分配，但同一样本可在不同阶段复标——P3 锚点靠这条）。

两个工程决定：JSON 列在生产用 JSONB、测试用通用 JSON，使建模不依赖本地 Postgres；建表 T1 用 `create_all`，**上线前（T5）引入 Alembic**，首个 revision 以当时模型为基线。

### 部署平台决策（2026-09-11）

继续用阿里云 ECS，理由是免费余额未用完 + V2 标注数据合规问题未定论。CSC Rahti 作为后备，触发条件为余额耗尽或需要机构可信域名招募欧洲标注员。迁移前置条件：(a) 确认标注员地域（决定延迟优劣），(b) 确认标注数据是否属个人数据——**Rahti 为 Minimum 保护级，官方明确不适合个人数据**。用户已有 Rahti 项目：`ai`、`annotate`(imigue_annotate)，但均非本人创建，部署前需确认角色权限。另注意 Rahti 强制非 root + 随机 UID，官方 nginx 镜像跑不起来。

### 尚未决定（不阻塞 T2–T3，P1 开跑前必须有答案）

1. 静帧人工核对由谁执行、排在什么时候（**3500 张**扫视式核对是 P1 前置工序；每个样本一张，不是每个模态一张）。
2. ~~锚点在每人队列中的占比~~ **已定**：`coverage=2`（每个锚点由 2 人标注），实得每人 30 个锚点、占队列 15.8%。见下方分配计划。
3. 正式标注采样频率 10 Hz 还是 20 Hz（影响 chunk 大小与存储量估算）。
4. 点击方形后是否先允许一次练习。
5. 多轮次最终采用哪一次（T7 的质量标记可支撑「研究者选择」）。
6. 文本逐词时间戳来源（当前 example 是按字符数近似匀速，正式标注需真实方案）。
7. 三个单模态呈现顺序是否按标注者轮换。
8. P2 training 的标注者人数。

---

## 每个 task 在标注界面上如何体现（2026-09-11）

T1–T3 全部做在水面以下，**标注者看到的页面尚未改变**；可见改动集中在 T4。

| Task | 状态 | 标注者看到什么 | 研究者看到什么 |
| --- | --- | --- | --- |
| T1 数据模型 | ✅ | 无变化 | 无变化。决定了平台"能不能做"：记住谁属于哪个阶段、样本该给谁标、排队第几、是否锚点，并为静帧与质量标记留位 |
| T2 API | ✅ | 无变化（未接线）。能力已就位：任务列表改为服务器按分配下发的**个人队列**；换设备用同一链接打开**进度还在**；提交后再改形成新版本，旧版不覆盖；锚点混在队列中**界面上看不出来** | — |
| T3 同步层 | ✅ | 无变化（未接线）。能力已就位：**断网可继续标注**，恢复后自动补传；服务器不可达时页面仍能打开并继续标 | — |
| T4 身份与界面返工 | ✅ | **第一次看见变化**：改用专属链接进入；侧栏变为"我的队列"；页面出现**说话人静帧**常驻提示"按这个人标注"；模态改为 仅视觉/仅音频/仅文本 → 完整视频最后；保存区显示"已同步 / N 条待上传" | — |
| T5 部署 | ✅ | 界面无感，但没有它标注者无法从外网访问 | 数据库自动备份 |
| T6 管理端只读 | ✅ | 无 | 自己的页面：各标注者完成到哪、谁卡住；一键导出全部结果（格式不变，导出即可画曲线） |
| T7 管理端可写 | ✅ | 无 | **生成并发放标注者链接**、分配样本、作废某轮、打质量标记 |

**关键排期含义**：发链接的能力在 T7，**T7 未完成前 P1 pilot 开不了跑**。T4 让标注者看见新界面 → T5 让他们能访问 → T6 让研究者看见进度 → T7 开跑 P1。P2 培训需等 V3。

**并行前置工序**：静帧预处理流水线不属于 T1–T7，需与之并行推进。即使 T7 完成、链接已可发放，**3500 张静帧未核对完则 P1 仍无法开始**，应尽早排期。

### T4 补充要求：说话人静帧的摆放（2026-09-11 用户指定）

- 放到页面**现有空白处**，不要挤占媒体区或标注方形区。
- **尺寸可以小**，够认清是谁即可。
- 注意排版协调。当前可用空白带：`workspace-heading` 右侧（`sample-counter` 旁）与 `instruction-strip` 一带，具体位置由 T4 实现时定。

---

## 2026-09-11 进度小结 — T1/T2/T3 完成 + 分配工具

### 已完成

| 项 | 验证 |
| --- | --- |
| T1 后端骨架与七张表 | pytest ✅ |
| T2 标注者侧 API（六个接口） | pytest ✅ |
| T3 `HttpRepository` + `SyncingRepository` | **vitest 已写未跑**（Roihu 无 node） |
| 分配工具 `manage.py` + `allocation.py` | pytest ✅ |
| 后端测试合计 | **49 个全绿** |

本次 commit：`7b599f9` `e64ccb8` `d40cf6c` `73b9ae6` `2de538b` `22c5ff1`。
**全部尚未 push**（本地领先 origin 12 个 commit）。

### ⚠️ 前端未验证

T3 的 `tests/httpRepository.test.ts`、`tests/syncingRepository.test.ts` 与 `tsc --noEmit`
**都没有在 Roihu 上跑过**——本机只有 Python，没有 node / node_modules。
接手方或用户须在本地 dev 环境跑 `npm test` 后再依赖 T3。

### 池子事实（已核实，此前 handover 记载有歧义）

- `annotation_pool_3500.jsonl` 3500 条，`sample_id` 唯一。
- `anchor_set_500.jsonl` 500 条，**全部是 3500 的子集**（交集正好 500）。
- 来源构成：meld 920 / chsims 919 / iemocap 877 / mosi 521 / mustard 263。
- 因此「300 锚点」= 从 500 中取 300，本就在 3500 池内。**此前"3500 + 500"的表述是错的，是 3500 含 500**。

### 分配的关键约束（容易做错）

锚点在池子里，所以**直接把 3500 均分给 20 人会让每个锚点只落到一个人手上，锚点即失效**。
必须分两步：3200 非锚点样本均分，300 锚点按 `--coverage` 重复发放给不同的人。

已生成并入库的 P3 计划（`server/plans/assignment_plan_P3.csv`，3800 行）：

- 20 位标注者，每人 **190 个样本**（≈760 个单模态任务），min=max 完全均衡
- 每人 **30 个锚点**，占队列 15.8%，散布在队列内部而非首尾
- 300 个锚点**每个恰好由 2 人标注**；无人拿到重复样本
- `--seed 20260911` 可完整重现

### 管理工具用法

见 `server/README.md`。四个命令：`create-annotators` / `plan` / `apply-plan` / `status`。

**设计要点：分配计划先出 CSV 再回填**，不直接写库。分配是研究设计决策，需人工复核；
CSV 可用表格软件直接改，改完 `apply-plan` 覆盖写入。

**账号 CSV 含明文 token，已 gitignore**；服务器只存哈希，文件丢失只能重建账号。

### 当前阻塞

1. `apply-plan` **还跑不了**：`tasks` 表里没有真实样本，需先完成素材导入（3500×4 模态预处理）。
2. 已生成的 20 个账号是**临时 SQLite 库里的演练版，链接打不开**；T5 部署出真库后须重跑
   `create-annotators` 才是正式账号。分配计划 CSV 不受影响，它只引用 sample_id 与 P3-01…P3-20。

### 下一步可选路径

- **T4**（身份接入 + 模态顺序返工 + 静帧展示）：纯前端，Roihu 上无法验证，建议在本地做。
- **T5**（部署）：Docker / Caddy / 备份，Roihu 上可验证，且是拿到正式账号的前提。

### 静帧需求的时点（2026-09-11 确认）

静帧数量按**样本**计：**3500 张**（每样本一张，四个模态共用同一张身份参考），
不是 3500×4。此前记作 3800 是把锚点重复计数了，已改正。

需求分两个时点：

| 时点 | 需要多少 | 用途 |
| --- | --- | --- |
| **T4 开发时** | **1 张**（示例样本 `meld_dia11_utt9`） | 做界面与测试，不需要批量 |
| **素材导入 / P1 开跑前** | **全部 3500 张** | `apply-plan` 要求 tasks 表有真实样本；静帧随素材一起入库 |

用户已确认静帧预处理**另写独立程序**，不并入标注平台代码。

---

## 🚩 P1 pilot 开跑前的硬性阻塞项（勿跳过）

按优先级排列，全部完成前不要让真人开始标注。

| # | 阻塞项 | 为什么不能跳过 |
| --- | --- | --- |
| 1 | **重写并跑通 Playwright e2e** | 现有 9 个用例按 V1 界面写死（`光影之间` 等旧侧栏标题、`搜索样本` 标签、已删除的标注者输入框），T4 后必然全红。它们覆盖的正是「刷新后草稿是否还在」「暂停时文字是否保持」「离开方形是否继续采样」这类**只有真浏览器能验证**的场景——这种 bug 在 pilot 里出现等于 8 个人的工时白费。顺带去掉 `playwright.config.ts` 里写死的 `D:\anaconda\…\node.exe` 与 `channel: "msedge"`，改为可在 ECS 的 node 容器里跑。 |
| 2 | **引入 Alembic** | T5 起 `create_all` 不会 ALTER 已有表。一旦库里有真实标注数据，再改 schema 就必须先有迁移，否则只能 drop 重建 = 数据没了。 |
| 3 | **素材导入 + 静帧全量** | `apply-plan` 要求 tasks 表有真实样本；3500 张静帧未核对完则队列里会指错人。 |
| 4 | **回填分配** | 20 个账号目前分配均为 0，标注者打开只会看到空状态。 |


---

## 2026-09-11 — T4–T7 全部完成，V2 功能收口

**线上地址**
- 标注页：`https://47.238.255.165.nip.io/annotation/?t=<标注者令牌>`
- 管理页：`https://47.238.255.165.nip.io/annotation/api/admin/ui`
- 管理员令牌：服务器上 `. /opt/xmer-label/.env.annotation && echo $XMER_ADMIN_TOKEN`

**测试**：后端 74 个 pytest 全绿；前端 30 个 vitest + `tsc --noEmit` 在 ECS 的
`node:20-alpine` 一次性容器里跑绿（Roihu 无 node 不再是阻碍，命令见 `deploy/README.md`）。

### T7 的三条设计原则（改动时勿破坏）

1. **重复建号会跳过已存在的编号，不换令牌。** 否则重跑一次就把已经发出去的链接全部作废。
2. **停用立刻生效**：`active=false` 后该令牌访问 `/api/me` 返回 403。已有测试守住。
3. **作废是追加一条处置记录，不删数据。** `attempt_flags` 保留完整处置历史，原始轨迹永远在——
   判断错了还能翻案。

### 分配逻辑已收敛到一处

`app/assignments.py` 的 `replace_assignments()` 由 CLI（`manage.py apply-plan`）与
管理端 API（`PUT /api/admin/assignments/{id}`）共用，避免两处各写一套"样本展开成四个模态
任务"而慢慢跑偏。**整体替换而非增量合并**：分配是一份完整计划，半新半旧的队列比错误的
队列更难排查。

### 排期更正

handover 早前写"发链接的能力在 T7，T7 未完成 P1 开不了跑"——那是在 `manage.py` 之前写的。
**账号生成与分配回填 CLI 早已可用**，T7 的独有价值是质量标记/作废，以及不用 SSH 在浏览器里操作。
P1 的真实阻塞项见上方 🚩 小节（e2e 重写 / Alembic / 素材与静帧 / 回填分配），与 T7 无关。

### 生产库现状

20 个 `main` 阶段账号，分配均为 0（等素材导入）。本次验证用的 2 个 P1 测试账号已删除。

---

## 2026-09-12 — 静帧交付接收与分配计划重生成

06-speaker-frames 交付 **3440 条**（3500 池剔除 60 条），交付说明见
`../06-speaker-frames/DELIVERY.md`，复核依据见 `human-review-report.md`。

### 接收时核对出的两件事

**1. 被剔除的 60 条里有 6 条是正在使用的锚点。** 原 `assignment_plan_P3.csv` 按 3500 生成，
引用了这些已剔除样本。**已重生成**：池子 3440，锚点**从存活的 489 条里重新取 300**
（不是降到 294——300 是既定设计且补得起）。新计划 3740 行，每人 187 样本 + 30 锚点，
300 个锚点各由 2 人标注，引用被剔除样本 **0 条**。`seed=20260912`。

**2. `speaker_name` 有占位符与编码残留。** `clean_speaker_name()`（`app/assignments.py`）
在**导入时**清洗，不在前端过滤——脏值进库后每个消费方（前端、导出、分析脚本）都得再过滤一遍。
规则：剔除纯占位符（PERSON / PERSON1 / PERSON3 / All / MODERATOR / MEMBER-GIRL，共 16 条），
修 Windows-1252 撇号残留（`Richard\x92s Date`）；**角色描述一律保留**——
`Flight Attendant`、`Policeman` 这类对认人有用，不是脏值。
实跑交付数据：清洗后有姓名 1112 条，无姓名 2328 条（68%）。

### 前端 null 姓名已实拍确认

67% 的样本没有姓名。`MediaPanel` 在 `speaker_name` 为 null 时只渲染静帧与
「目标说话人 / 按这个人进行标注」，无空元素、无错位。已截图验证，非推断。

### 交付集的已知限制（来自 DELIVERY.md）

- **iemocap 874 条人脸像素偏低**（中位 48px，放大到 512×512 会糊）。保留理由是单人半身固定机位、
  画面里没有第二个人可混淆。若上线后觉得影响使用，建议**调小这批的展示尺寸**而非剔除。
- `frame_path` 是**相对 `out/` 的路径**，拼绝对路径需加
  `/scratch/project_2017416/yyy2026/XMER/06-speaker-frames/out/`。

### 素材导入仍被一个决定卡住

文本逐词时间戳的来源（人工 / 字幕文件 / 强制对齐）未定。示例样本用的是按字符数近似匀速分配，
正式标注不能沿用。这个定了才能写预处理流水线。

---

## 2026-09-13 — Alembic 接管表结构 + 全量素材就绪

### 表结构改由 Alembic 管理（基准 `a5434311ec63`）

`create_all` 已从容器启动路径移除，改为 `alembic upgrade head`。生产库已 `stamp` 在基准版本，
20 个账号与既有数据完好。详见 `deploy/README.md` 的「表结构与迁移」一节。

**改了 `app/models.py` 必须生成迁移**，`tests/test_migrations.py` 会拦住漏生成的情况。

**Alembic 只管表的形状**：重新分配样本、替换标注数据都走应用层，与迁移无关。

### 全量素材已就绪（尚未上传 ECS）

| 产物 | 位置 | 规模 |
| --- | --- | --- |
| 词级对齐 | `07-text-alignment/out/align/` | 3440/3440 零失败，47529 token |
| 四模态素材 | `08-material-prep/out/media/` | 3440 样本，**7.4 GB** |
| 转录稿 | `08-material-prep/out/transcripts/` | 3440 份，全部通过标注页校验 |
| 导入清单 | `08-material-prep/out/tasks_import.json` | **13760 个任务**，6.5 MB |

**文本时间戳方案已定：强制对齐，不做语音识别。** 模型只把数据集已有的转录稿摆到时间轴上，
改不了词——标注者读到的句子必须逐 token 等于数据集转录稿，否则与 04 的冲突标签对不上。
`07-text-alignment` 里有硬断言守这条。听审页（21 条含笑声样本）：
<https://claude.ai/code/artifact/6b1a87a5-e979-4998-b111-8bbb88319886>

### 素材处理的三处特殊情况（改动流水线时勿踩）

1. **MUStARD 的 `video.mp4` 没有音轨**，声音在同目录 `audio.wav`，完整视频要手动合入。
2. **MELD 音轨是 5.1 六声道**，不下混浏览器放不出声。
3. **转录稿 token 时长必须为正**：插值定位的标点是零时长，直接落盘会被标注页整个拒收。

### 平台坑：登录节点 ARM，计算节点 x86

`small`/`test` 分区是 **x86_64**，登录节点与 GH200 GPU 分区是 **arm64**。
pytorch 与 ffmpeg 模块都是 ARM 容器，在 x86 计算节点上**跑不起来也拿不到**。
对齐作业投 `gpumedium`（ARM + GPU，3440 条约 2 分钟）；素材处理只用 ffmpeg，
在登录节点 8 进程并行约 20 分钟。

### 下一步

1. 上传 7.4 GB 素材到 ECS（`/opt/xmer-label/annotation-static/media/pool/`）
2. `manage.py import-tasks --file tasks_import.json`（8 → 13768 条任务）
3. `manage.py apply-plan --plan plans/assignment_plan_P3.csv --phase main`
4. 🚩 重写 Playwright e2e —— P1 开跑前的最后一个阻塞项

---

# 2026-09-13 — 标注平台建设总结（V2 收尾）

在线版（排版更好读）：<https://claude.ai/code/artifact/b5ce0ea5-ff36-486b-b0f6-7ab9d3a6a713>

从一个只能存在浏览器里的单机页面，做到 20 个人可同时开工的云端平台，外加 3440 个样本的全套素材。

## 一眼看懂

| 阶段 | 做的事 | 标注者会看到 | 研究者会看到 |
| --- | --- | --- | --- |
| T1 数据模型 | 建七张表 | 无 | 无。决定平台以后能做什么 |
| T2 标注接口 | 六个接口，存取标注 | 无（未接线） | 无 |
| T3 同步层 | 本地先写、后台上传 | 无（未接线） | 无 |
| T4 界面接线 | 专属链接、模态换序、静帧、同步状态 | **第一次看到变化** | 无 |
| T5 部署 | 后端上线、独立库、每日备份 | 能从外网打开 | 数据有备份 |
| T6 管理端只读 | 进度总览、导出 | 无 | **第一次有自己的页面** |
| T7 管理端可写 | 发号、分配、作废、质量标记 | 无 | 能在网页上管人管数据 |
| 分配工具 | 20 人分配计划 | 各自的队列 | 可改的 CSV 计划 |
| 说话人静帧 | 3440 张，人工核对过 | 「按这个人标注」的小图 | — |
| 文本时间戳 | 词级强制对齐 | 文字跟着语速出现 | — |
| 素材预处理 | 3440×4 模态，7.4 GB | 四种材料能播 | — |
| 上线与导入 | 素材上传、任务入库、分配回填 | **打开就能开工** | 进度页有数了 |
| Alembic | 表结构改动不再丢数据 | 无 | 无。保命用的 |
| 端到端测试 | 11 个用例，真浏览器 | 无 | 无。防的是标注者白干 |

## 各阶段要点

**T1–T3 地基**：标注者完全看不到，但后面所有功能站在上面。约束写进数据库本身而非只靠代码自觉；
轮次/采样块/提交编号都由浏览器生成，断网重试重发同一编号，服务器认出是重复；队列里不暴露锚点；
同步层不替换本地存储而是加一层，先落本地再后台推。

**T4–T5 上线**：删掉可随便改的标注者编号输入框（云端等于谁都能冒充）；令牌读出后立即从地址栏抹掉；
**完整视频从第一位挪到最后一位**（先看完整视频会让后续单模态标注变成回忆而非感知）；
复用现有服务器新建独立库；每日备份且仅在转储非空时轮换。

**T6–T7 管理端**：进度按任务去重计数（同一任务两版算一个任务）；没有分配的账号也要列出来
（否则被漏掉的人会直接从列表消失）；重复生成账号跳过已存在编号**不换令牌**；停用立刻生效；
作废是追加处置记录**不删数据**。

**数据准备**：五个数据集说话人识别难度差一个数量级，没有统一用同一种贵办法；
文本时间戳**用强制对齐不用语音识别**（模型只能决定词落在哪一刻，改不了词）；
视频一律直接拷贝不重编码；四模态共用同一时长。

**保命两件事**：Alembic 在真实数据进库前接管表结构；端到端测试用独立账号跑、跑完连数据一起删。

## 踩过的坑（按影响排序）

1. **配置缺失会静默写错库**——数据库地址没配时退回本地文件，而部署命令少带参数正好让它变空。数据写进容器里没人看的文件，表面完全正常。已改成缺配置直接起不来。
2. **服务器把完整视频排第一个下发**——同一样本四任务排序号相同、没定义第二排序。侧栏只在显示时排序，默认进入和「下一个」都跟服务器顺序走，防污染的改动被绕过去了。
3. **对齐实现在漏词**——担心的是识别出错，实际是规范化把数字清成空串。漏掉数字还会让后面所有词的时间偏移。已改为数字先展开、绝不丢词并加硬检查。
4. **剔除的 60 条静帧里有 6 条是在用的锚点**——分配计划引用了不存在的样本，已重生成并把锚点补回 300。
5. **同一轮次能提交两次**——换个提交编号就凭空多一版。浏览器端本来禁止，服务器端漏了。
6. **rsync --delete 会删掉素材**——前端部署命令带 `--delete`，素材若放在同一目录会被整个删除。已移到独立目录 + 独立路由。
7. **改路由配置不会自动生效**——配置是挂载进去的，容器不重建就还是旧规则，必须显式重载。
8. **接口路由必须排在静态页路由之前**，否则被静态文件服务吃掉。
9. **登录节点 ARM、计算节点 x86**——多数软件模块在 x86 计算节点上拿不到。对齐作业改投 GPU 分区。
10. **两个数据集的媒体特殊**：一个视频文件没有音轨（声音在旁边单独文件），一个是 5.1 六声道不下混放不出声。
11. **转录稿 token 时长必须为正**——插值定位的标点是零时长，直接落盘会被标注页整个拒收。
12. **时间戳丢时区**——首次返回带 UTC 标记、重试返回不带，客户端只能猜。

### 三次我自己的测试出了问题，值得记住

- **测试靠运气通过**：为"顺序未定义"写的回归测试恰好因插入顺序而通过。不刻意制造坏顺序，测试本身不可靠。
- **探针给假阳性**：测节点能否跑素材处理的脚本打印成功就退出，但那只是脚本正常结束，每条记录其实都失败了。该检查记录状态而非退出码。
- **首轮 e2e 三个失败全是用例写错**：选择器写错、拿没有静帧的演示样本去验静帧、点了已展开的分组把它折叠。

## 当前状态

| 项目 | 数量 | 状态 |
| --- | --- | --- |
| 样本 | 3440 | 四模态素材齐全，已上线 |
| 任务 | 13760 | 已入库 |
| 分配 | 14964 | 已回填，覆盖 20 人 |
| 每人工作量 | 约 188 样本 | 其中 30 个锚点 |
| 锚点 | 300 | 每个由 2 人标注 |
| 后端 / 前端 / 端到端测试 | 92 / 30 / 11 | 全绿 |
| 已提交标注 | 0 | 等人开工 |

**软件与数据均已就绪，20 个标注者打开各自链接即可开始。**
早前 🚩 标记的四项 P1 阻塞（e2e 重写 / Alembic / 素材与静帧 / 回填分配）**已全部清空**。

---

# 2026-09-13 夜 — Yoe 亲自试标 20 样本后的反馈（V3 改进清单）

V2 完成后，Yoe 用 `YOE-01`（pilot 阶段，20 样本 / 80 任务）亲自走了一遍完整标注流程。
以下为原始反馈整理，**尚未分析、尚未动手**，是 V3 的输入。

## A. 页面布局（影响每一次标注）

1. **保存 / 重新标注 / 导出 JSON 三个按钮要挪到标注罗盘右上角**。
   现在标注完要往下滚到页面底部才能操作，每个任务都滚一次，很烦。
   目标是**在同一屏内完成一次标注的全过程**。
2. 下方的标注时间、轮次信息等改为**可选择性查看**，不默认占位。
3. **左侧样本目录要固定**，不要随右侧内容一起向下滚动。

## B. 仅文本模态 —— 问题最严重，Yoe 明确说「text 是最难标的」

4. **`meld_dia762_utt2` 的文本没有显示完整**：标到后面文字就没有了。原因待排查。
5. **`meld_dia923_utt2` 长句显示崩坏**：文字虽然出全了，但
   - 词与词之间**没有空格**
   - 太长的句子**不换行**，后半句直接跑到画面外看不见
6. **呈现方式建议改掉**：不要逐词浮现，改成**整句先放上去，用高亮标出当前说到哪个词**。
   Yoe 的原话是逐词「动得太快就不太好标」。

## C. 素材呈现

7. **chsims 的无声视频自带硬字幕**，在「仅视觉」模态里**提前暴露了文本内容**——
   这直接破坏单模态隔离。建议**把画面下边缘裁掉一条**。
8. **iemocap 视频画面太小，上下左右都有黑边**，建议裁剪掉黑边再呈现。
9. **`chsims_video_0053_0006` 需要用 0.5 倍速才标得动**，因为有很多微表情和细微变化。
   可能意味着默认倍速策略或提示需要调整。

## D. 目录与进度口径

10. **侧栏不要再用「真实样本」这个名字**。给 3440 个样本各自一个**唯一编号 ID**，
    侧栏只显示 ID；样本对应哪个数据源记录在后台 CSV 里，管理员知道即可。
11. **进度按样本计数，不按任务计数**：20 个样本即使有 80 条曲线，也显示 `15/20`。
    进度条可以按 80 个任务的粒度走，但**数字要显示样本数**。
    并且**一个样本的四个子任务只标完两个，不算这个样本完成**。

## 备注

- 试标账号 `YOE-01` 为 pilot 阶段，与 20 个 main 阶段正式账号完全隔离，数据不混。
- 标注结果已在云端，可用管理端接口直接取：
  `GET /api/admin/export?annotator=YOE-01`，格式与 `scripts/plot_va_curves.py` 兼容。
- **Yoe 明确要求：先不要分析，等他本人一起看。**

---

# 2026-09-14 — V3 第一轮：按 Yoe 试标反馈改造

昨夜那份 11 条反馈（见上一节）分成四批推进。本节记录**已完成的第 1–3 批**与
第 4 批的执行情况。三个方案分歧点由 Yoe 拍板：整段显示文本、只裁 visual、
试标的 20 条不排除在素材重做之外（并入锚点集，待办）。

## 第 1 批 · 文本模态改为整段常驻 + 高亮（反馈 4/5/6）

**一个根因带出三个症状。** `textAt()` 用 `join("")` 拼接词元——这是为中文写的，
英文因此拼成 `AndImjustgettingoutofamarriage` 一整个"超长单词"：既没有词间空格，
浏览器也找不到断行点，长句直接冲出画面。同一个函数只返回**当前时刻所在的那一句**，
所以句子讲完画面就空了。全库 3440 条里有 **166 条尾部静默超过 1.5 秒**，都会空屏。

改成 `transcriptTokens()`：整段转录稿一次给全，每个词带 `spoken` 标记和
`lead` 分隔符（中文两侧不加空格，英文加）。播放时只有高亮在推进，文字本身不动。

**`meld_dia762_utt2` 的"文字显示不全"追到了源头，不是我们的 bug。**
MELD 原始 CSV 里这条就是 `"Look at me! I'm Chandler! Could I"`——数据集自带的截断
（原话是 "Could I BE wearing any more clothes?"）。04 的池子、07 的对齐、08 的转录稿
都忠实照搬，没人丢词。能修的只有显示层：改成整段常驻后，至少不会再整屏空掉。

## 第 2 批 · 一屏内跑完一次标注（反馈 1/2/3）

- 保存 / 重新标注 / 导出三个按钮抽成 `ResultActions`，挪进标注罗盘的标题栏右上角
- `AttemptPanel` 整体改成 `<details>`：标题行只留状态、采样点数、提交版本，
  轮次列表和时间戳默认收起
- `.sidebar` 加 `position: sticky; top: 0; height: 100vh`，右侧滚动时目录不动

## 第 3 批 · 样本编号与进度口径（反馈 10/11）

**新增 `Task.display_id`**（迁移 `22a6741867d8`，基于基准 `a5434311ec63`）。
`manage.py assign-display-ids` 发放 `S0001`–`S3441`，**编号顺序是打乱的**：
按字母序发号会让 S0001–S0917 全是 chsims、往后整段 iemocap，标注者从编号区间
就能猜出数据来源。已发出的编号绝不改动，增量导入的新样本往后接。

映射表 `server/plans/display_ids.csv` 是**唯一**能把 S0001 还原回
`meld_dia762_utt2` 的东西，分析阶段离不开，务必随实验数据一起留存。

`TaskOut` 同时**去掉了 `source_id` 和 `title`**——两者都含数据集名，下发给前端
等于白给。侧栏搜索也改成只匹配编号和模态名。

> 局限：`task_id`（`chsims_xxx::visual`）和媒体 URL 仍带原始编号，打开开发者工具
> 能看到。做到完全不透明要连媒体目录一起改名，成本远大于收益——当前目标是
> 防止**肉眼识别**，不是防技术手段。

**进度改按样本计。** 数字显示 `15 / 20 个样本`，一个样本四个模态全部提交才算完成；
进度条仍按 80 个任务的细粒度走，这样标完一个模态也能看到条在动。

## 第 4 批 · 素材重裁（反馈 7/8）

`08-material-prep/src/recrop_visual.py`，**只重做 `visual.mp4`**：完整视频模态
本来就给声音，字幕算不上泄漏，而重编码一次就掉一次画质。原件备份在
`out/visual-backup/`，可随时还原。

- **chsims 917 条**裁掉画面下缘 18%。硬字幕**不按前缀分布**（`aqgy` 和 `video`
  两类都出现过），所以不做检测、全部一刀切。
- **iemocap 874 条**统一 `crop=344:236:8:123`。874 条全是 360×480，
  采样 8 条 cropdetect 结果一致（`346..350:238:6..8:122`），取交集再内缩 2px。

### 两个坑

**1. 登录节点上有两个 ffmpeg，只有一个能编码。**
`module load ffmpeg/7.1`（spack）**不带 libx264**，`-crf` 直接报
`Unrecognized option 'crf'`；`module load python-data/3.12-20.04` 自带的
ffmpeg 8.0.1 才能编。后加载的模块覆盖 PATH——**加载顺序决定用哪个**。

**2. 约 2% 的片源容器时长虚报。** 起初按"前后时长一致"校验，20 条 chsims 报
"漂移 4.040 → 3.880"。查下去发现**原始数据集文件本身**只有 96 帧可解码
（96/25 = 3.84s），而容器头写着 4.04s；原来的流拷贝把这个虚报值一起抄了过来，
重编码才让它现形。**帧数才是守恒量**，校验改成前后帧数必须完全相等。

### 差点漏掉的连带影响：任务时长必须跟着素材一起改

`session.ts` 在 `loadedmetadata` 时核对媒体实际时长与任务清单，
**差超过 0.25 秒就报「媒体时长无效或与任务清单不一致」并拒绝打开这个任务**。
重编码让 270 条样本的 visual 时长回落到真实值，其中 **5 条偏差超过 0.25 秒**
（最大 0.56 秒）——不处理的话这 5 个样本的"仅视觉"任务谁都打不开。

为此新增 `manage.py set-durations --file {task_id: 秒数}`，用实测值回填，
本次更新 900 个任务。**以后凡是重新处理素材，都要跟着跑一次这个命令。**

### 本批产物与落地

- 重裁后 1791 个 `visual.mp4` 共 798 MB（原 2.5 GB，crop + crf20 重编码后变小），
  已增量同步到 `/opt/xmer-label/annotation-pool/media/`
- 原件备份 `08-material-prep/out/visual-backup/`（Roihu 上，未上传）
- 重裁日志 `out/recrop.jsonl` + `out/recrop-retry.jsonl`，逐条含 crop 参数与前后帧数

## 尚未处理

- **反馈 9（倍速）**：让倍速选择跨任务保持、并在指导语里提示"微表情细微的样本可以放慢"。
- **Yoe 试标的 20 条并入锚点集**：需改 04 的锚点清单并重跑分配计划。
- **V3 正题**：曲线回放与延迟校准，P2 training 依赖它。

## 测试现状

后端 96 passed（新增编号 4 条）、前端 32 passed、e2e 13 passed
（新增「目录只显示不透明编号」与「进度按样本计数」两条）。

## 2026-09-14 续 · 倍速保持与锚点集扩到 318

### 倍速跨任务保持（反馈 9）

倍速选择存进 `localStorage`（`xmer.playback_rate`），换任务、刷新都保持。
存储被禁用（隐私模式）或值不在 `PLAYBACK_RATES` 里时退回 1 倍速，不抛错。
**没有加指导语**——Yoe 要求页面保持简洁。

### Yoe 试标的 20 条并入锚点集

`YOE-01` 标过的 20 个样本转为锚点，其中 2 条本来就是锚点，
新增 18 条，**锚点集 300 → 318**。

| | 旧 | 新 |
| --- | --- | --- |
| 池子 | 3440 | 3440 |
| 锚点（每条 2 人标） | 300 | 318 |
| 单人标注样本 | 187 | 187–189 |
| 分配行数 | 3740 | 3758 |
| 落库任务数 | 14964 | 15036 |

重排前确认过 **20 个 main 账号全部零提交零轮次**，替换分配不会毁数据；
`YOE-01` 属 pilot 阶段，不受影响。

回填后独立校验：3440 条全覆盖、318 个锚点各恰好 2 人、非锚点各 1 人、
**没有任何锚点的两份落到同一个人身上**、Yoe 那 20 条全部在锚点集内。

操作留痕：
- `server/plans/anchor_set_318.jsonl`（带 `yoe_pilot` 标记，认得出哪 20 条来自试标）
- `server/plans/pool_3440.jsonl`
- `server/plans/assignment_plan_P3.csv`（已更新为新计划，seed 20260914）

### 测试现状

后端 96 passed、前端 34 passed（新增倍速记忆 2 条）、
e2e 14 passed（新增「倍速在换任务和刷新后保持」）。

## 2026-09-14 续二 · 曲线内置又撤下

按 Yoe 要求把标注曲线内置到罗盘下方的空白处，先做成「本轮一条」，
再按他的选择改成「四个模态全部提交后叠加显示四条」。**上线后 Yoe 觉得不好看，撤掉了。**

页面上的曲线（`CurvePanel`、相关样式与 e2e 用例）已整块移除，
要还原从 `dbe35e1` 取。

**为它加的两样东西保留下来了**，V3 的曲线回放与延迟校准正好要用：
- `GET /api/attempts/{attempt_id}/samples` —— 按轮次读回采样点，按 sample_index 排序，
  取别人的轮次返回 404
- 存储层 `attemptSamples`：本地走 IndexedDB 的 attempt 索引，云端走上面这个接口，
  `SyncingRepository` 沿用「先远端、失败回退本地」

**一处教训**：`getByRole("button", { name: /提交|更新/ })` 同时命中了侧栏的
「已提交」筛选标签，e2e 报 strict mode 冲突。按语义角色选元素时要留意同名控件。

**一处「看图不如查数」**：截图里四条曲线几乎重合，我以为渲染串了轮次；
拉服务器原始数值一对，四条曲线本来就几乎一样——截图脚本四次扫的 x 轨迹相同，
而 x 就是效价。渲染没问题，是测试数据的问题。

## 2026-09-14 续三 · Training session（4 人 + 导师）

Yoe 先做一轮 training session 再开 V3：找 4 位师弟师妹各标 10 个样本，
**两两配对共用同一组 10 个**，加上 Yoe 自己的 pilot 标注，**每个样本 3 份**。
另给导师一个含全部 20 个样本的账号。

- 账号：`TR-01`–`TR-04`（每人 40 个任务）、`SUP-01` 导师（80 个任务），阶段均为 `training`
- 样本就是 `YOE-01` 标过的那 20 个，**按数据源交错均分**成 A/B 两组，
  两组的来源构成尽量接近（A: chsims 3 / iemocap 2 / meld 2 / mosi 2 / mustard 1；
  B: meld 3 / chsims 2 / iemocap 2 / mosi 2 / mustard 1）
- 配对：TR-01+TR-02 → A 组，TR-03+TR-04 → B 组
- **每人的样本顺序单独打乱**，同一对的两个人顺序不同，免得顺序效应和人混在一起
- 分配行标了 `is_anchor=1`：这 20 个本来就是重复标注的样本

留痕：`server/plans/training_session_plan.csv`（回填用）、
`training_session_groups.csv`（人看的对照表，含每个样本由谁标）。
**明文令牌不入库**，只在创建时输出过一次；丢了用 `manage.py reissue-token` 重发。

回填后独立校验过：两两配对的样本集合完全相同、两组无重叠、
合起来正好等于导师的 20 个、也正好等于 YOE-01 的 20 个，每个样本恰好 3 人。

## 2026-09-14 续四 · 备份池 939 条（已挑选，未处理）

Yoe 担心 3440 条在后续删减后不够用，要求从原始池子里再挑一批备用。
**只做选样，不跑流水线**；是否启用由 Yoe 与导师在 **P3 大规模标注开始前**敲定。
👉 **到时候要主动提醒他。**

### 池子的硬约束

3500 之外、符合主集口径（双 judge 信心 ≥ moderate + 构成冲突）的剩余量：

| 源 | 交集层 | 分歧层 | 合计 |
| --- | --- | --- | --- |
| meld | 1427 | 1328 | 2755 |
| chsims | 160 | 540 | 700 |
| iemocap | **0** | 250 | 250 |
| mosi | **0** | 121 | 121 |
| mustard | **0** | **0** | **0** |

**mustard 彻底见底**——质检池总共才 496 条，263 条已进主集，其余基本被双方判为无冲突。
偏偏它是删除率最高的源：已删的 60 条里 mustard 占 20 条（20/263 = **7.6%**），
而 meld 3.8%、iemocap 0.3%、chsims 0.2%。**最需要备份的源一条都不剩**，
这是 MUStARD 数据集本身的规模限制，抽样策略解决不了。

Yoe 的决定：**mustard 不管，整个排除。**

### 产物

`sample_backup_pool.py` → `data/backup_pool.jsonl` + `.meta.json`，**939 条**，seed 20260914。

配额按**主集各源占比等比缩放**、再受剩余量上限约束（没用主集那套注水法：
交集层只剩 meld 和 chsims，注水会让备份池近七成是 meld，结构和主集不可比）：

| 源 | 主集占比 | 备份占比 | 条数 | 其中交集 |
| --- | --- | --- | --- | --- |
| chsims | 28.4% | 30.2% | 284 | 160 |
| meld | 28.4% | 30.2% | 284 | 227 |
| iemocap | 27.1% | 26.6% | 250 | 0 |
| mosi | 16.1% | 12.9% | 121 | 0 |

缺口：iemocap 少 21、mosi 少 40，都是剩余候选不够。
**分层比例是 intersect 387 / disagree 552，与主集的 80/20 不同**，分析时要注意。

校验（独立于脚本自身输出重跑）：无重复、与 3500 零重叠、与已删 60 条零重叠、
无 mustard、信心全部达 moderate、分层标注正确、全部构成冲突。

### 真要启用还得跑

说话人静帧（Gemini + Yoe 人工核对）→ 词级强制对齐 → 四模态素材 → 上传 →
`import-tasks` → 重做分配计划。约为现有工作量的 **27%**，静帧那步需要 Yoe 再核对一轮。
